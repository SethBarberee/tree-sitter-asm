[
 ","
] @punctuation.delimiter

(comment) @comment @spell

(register) @identifier
(constant) @constant

(func_name) @function
(label) @label

[
    (load_opcode)
    ;(ldm_opcode)
    (branch_opcode)
    (push_opcode)
    (opcode)
 ] @keyword

(ERROR) @error
