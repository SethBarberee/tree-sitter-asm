[
 ","
] @punctuation.delimiter

(comment) @comment @spell

(register) @identifier

(func_name) @function
(label) @label

[
    (load_opcode)
    (branch_opcode)
    (push_opcode)
    (opcode)
 ] @keyword

(ERROR) @error
