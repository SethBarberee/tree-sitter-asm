[
 ","
] @punctuation.delimiter

(comment) @comment @spell

(register) @parameter
(constant) @constant

(func_name) @function
(label) @label

[
    (load_opcode)
    (ldm_opcode)
    (adr_opcode)
    (branch_opcode)
    (push_opcode)
    (opcode)
 ] @keyword

(ERROR) @error
