=================================================
basic function label 
=================================================

test:

------------------------------------------------

(source_file
  (function_definition
    (function_label)))

=================================================
basic addition function
=================================================

test:
    add r0, r0, r1
    bx lr

------------------------------------------------

(source_file
  (function_definition
    (function_label)
    (math_statement
      (opcode)
      (register)
      (register)
      (register))
    (return_statement
      (register))))

=================================================
basic function w/ push and branch
=================================================

test:
    push lr
    mov r0, 0
    bl MemoryAlloc
    bx lr

------------------------------------------------

(source_file
  (function_definition
    (function_label)
    (simple_statement
      (opcode)
      (register))
    (simple_statement
      (opcode)
      (register)
      (constant))
    (branch_statement)
    (return_statement
      (register))))

