=================================================
basic function label 
=================================================

test:

------------------------------------------------

(source_file
  (function_definition
    (function_label)))

=================================================
basic addition function
=================================================

test:
    add r0, r0, r1
    bx lr

------------------------------------------------

(source_file
  (function_definition
    (function_label)
    (math_statement
      (opcode)
      (register)
      (register)
      (register))
    (return_statement
      (register))))

=================================================
basic mov w/ constant function
=================================================

test:
    mov r0, 0
    bx lr

------------------------------------------------

(source_file
  (function_definition
    (function_label)
    (mov_statement
      (opcode)
      (register)
      (constant))
    (return_statement
      (register))))

=================================================
basic function w/ branch
=================================================

test:
    bl MemoryAlloc
    bx lr

------------------------------------------------

(source_file
  (function_definition
    (function_label)
    (branch_statement)
    (return_statement
      (register))))


